#include "main.h"

/**
 * _abs - Determines the absolute value of an integer.
 * @z: The integer to be determined
 *
 * Return: The absolute value of the integer.
 */
int _abs(int z)
{
	if (z >= 0)
	{
		return (z);
	}
	else
	{
		return (-z);
	}
}
