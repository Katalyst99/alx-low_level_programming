#include "main.h"

/**
 * mul - A function that multiplies two integers.
 * @a: first varialbe
 * @b: second variable
 * Return: multiplication
 */
int mul(int a, int b)
{
	int mul = a * b;

	return (mul);
}
