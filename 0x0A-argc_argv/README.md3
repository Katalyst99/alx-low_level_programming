a program that multiplies two numbers
program should print the result of the multiplication, followed by a new line
assume that the two numbers and result of the multiplication can be stored in an integer
If the program does not receive two arguments, program should print Error, followed by a new line, and return 1
